<Window xmlns:oxy="http://oxyplot.org/wpf"  x:Class="MonteCarlo.Task4"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MonteCarlo"
        mc:Ignorable="d"
        Title="Задание 4" Height="600" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="4.5*"></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition></ColumnDefinition>
                <ColumnDefinition></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition></RowDefinition>
                    <RowDefinition Height="3*"></RowDefinition>
                </Grid.RowDefinitions>
                <TextBox x:Name="pointsCount" TextAlignment="Center" Margin="30,0,30,0"/>
                <Button x:Name="generateButton" Margin="10" Grid.Row="1" Click="generateButton_Click">Сгенерировать точки</Button>
            </Grid>

            <Grid Grid.Column="1">
                <Button x:Name="exactCalc" Grid.Row="0" Margin="5" Click="pointcountCalc_Click">Количество точек внутри фигуры</Button>
            </Grid>
        </Grid>
        <oxy:PlotView x:Name="plot" Width="{Binding ActualHeight, ElementName=plot, Mode=OneWay}"/>
    </Grid>
</Window>
